
server.port=8080
# Port = 0 would be random
# Set port to whatever you want (default 8080)
server.servlet.context-path=/boot

# Actuator Settings
management.endpoint.health.show-details=always
management.endpoints.web.exposure.include=*